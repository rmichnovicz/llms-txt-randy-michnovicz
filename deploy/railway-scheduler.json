{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE" },
  "deploy": {
    "startCommand": "brief schedule",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
